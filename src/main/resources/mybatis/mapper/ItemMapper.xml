<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.springboot.wearwave.mapper.ItemMapper">
	<!-- 모든 상품 가져오기 -->
	<select id="getAllItems" resultType="com.springboot.wearwave.model.Item">
        SELECT item_id, item_code, item_title, content, price, imagename
        FROM items_tbl
    </select>
	
	<!-- item_code를 사용하여 해당 상품 조회 -->
    <select id="getItemById" parameterType="string" resultType="com.springboot.wearwave.model.Item">
        SELECT item_id, item_code, item_title, content, price, imagename 
        FROM items_tbl 
        WHERE item_code = #{item_code}
    </select>

    <select id="getItemMainPage" parameterType="Integer" resultType="com.springboot.wearwave.model.Item">
        SELECT item_id, item_title, price, content
        FROM items_tbl
        WHERE item_id = #{item_id}
    </select>
</mapper>